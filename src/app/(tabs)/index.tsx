import { FoundationScreen } from '@/components/ui/foundation-screen';

export default function BookFoundationScreen() {
  return (
    <FoundationScreen
      title="Book"
      body="Native booking flow comes next. Pricing, route verification, booking creation, and payment state remain authoritative on Yard's backend."
    />
  );
}
